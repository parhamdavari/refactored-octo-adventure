# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

The import above holds commands, architecture, and conventions (manually maintained). This is the marketing/landing site for **Payesh**, the crypto-deposit-monitoring Telegram bot — the bot itself lives in the sibling `payesh` repo; product context is in `business-logic.md`.

To add or reorder a landing section, edit `src/pages/index.astro` (the only page) — each section is a self-contained `.astro` in `src/components/`, most rendered through the shared `content-section.astro` wrapper.
